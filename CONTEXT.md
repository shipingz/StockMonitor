# CONTEXT — A股 15分钟K线 MA60 监控项目

> 术语表与项目上下文。本文件随讨论持续更新。

## 项目定位

在 GitHub Actions 上定时运行，用 AKShare 抓取 A 股数据，检测「15分钟K线收盘价超过 MA60」的信号，通过企业微信群机器人和/或飞书群机器人 Webhook 推送通知（有哪个推哪个，ADR-0014）。

## 术语表

| 术语 | 定义 |
|---|---|
| **A股** | 中国大陆交易所上市的股票（沪深为主；北交所股票待定，见 ADR）。 |
| **15分钟K线** | 以 15 分钟为周期聚合的K线。A股交易时段 9:30–11:30、13:00–15:00，每天 16 根；每根在整点后 15 分钟收盘（9:45、10:00、…、11:30、13:15、…、15:00）。 |
| **收盘价** | 单根K线结束时（15分钟末）的最新成交价，即该K线的 close。 |
| **MA60（15分钟）** | 最近 60 根 15 分钟K线收盘价的简单移动平均（SMA）。60 根 ≈ 15 小时 ≈ 3.75 个交易日。 |
| **上穿（cross above）** | 当前K线收盘价 > MA60（含当前K线），且上一根K线收盘价 ≤ 上一时刻的 MA60（前 60 根，不含当前K线）；实现见 ADR-0002。 |
| **自选股列表** | 项目监控的股票集合，存放在 GitHub Repository variables `STOCK_LIST`（逗号分隔纯 6 位代码），上限 50 只（ADR-0001/0009）。 |
| **企业微信 Webhook** | 企业微信群机器人提供的 HTTP 推送接口，向群内发送 text/markdown 消息。Webhook URL 属机密，存于 GitHub Secrets。 |
| **飞书 Webhook** | 飞书群自定义机器人提供的 HTTP 推送接口，支持 interactive 卡片（lark_md 渲染）与 text。Webhook URL 属机密，存于 GitHub Secrets；可选「加签」密钥 `FEISHU_WEBHOOK_SECRET`。 |
| **GitHub Actions cron** | GitHub Actions 的定时调度（`schedule` 事件），最小粒度 5 分钟，实际触发有 30~60 秒延迟，时区为 UTC。 |
| **AKShare** | 开源 Python 金融数据接口库，本项目的数据源封装层。 |
| **交易时段调度** | 只在交易日（周一至周五、非节假日）的 A股交易时段内运行检查；节假日需用交易日历剔除。 |
| **指数退避重试** | 请求失败（429/超时/网络错误）时，按递增间隔自动重试的容错策略。 |

## 已确认决策索引

| 编号 | 决策 | 状态 |
|---|---|---|
| ADR-0001 | 监控范围：自选股列表（≤50只），而非全市场扫描 | 已确认 |
| ADR-0002 | 触发语义：A型「上穿」触发 + MA60 口径1（含当前K线） | 已确认 |
| ADR-0003 | 调度方式：收盘后 3 分钟精确对齐（16 时刻显式 cron），取代轮询 | 已确认 |
| ADR-0004 | 分钟数据源限制：`stock_zh_a_hist_min_em` 仅返回近 ~5 个交易日，历史不足 61 根（60 根 MA + 上一根对比）时跳过该股 | 已确认（事实约束） |
| ADR-0005 | 状态设计：无跨运行状态；去重由调度频率天然保证，接受边界重复/漏检 | 已确认 |
| ADR-0006 | 节假日剔除：交易日历判断 + 「最新已收盘K线」日期/新鲜度护栏（15:03 轮不误杀）；调休周末不覆盖，手动触发 | 已确认 |
| ADR-0007 | 消息格式：markdown；多信号合并推送；超限按贪心拆分（≤4096 字节，安全阈值 ~3800） | 已确认 |
| ADR-0008 | 失败告警分级：单股失败静默；整轮失败/日历失败/严重延迟推告警 | 已确认 |
| ADR-0009 | 配置管理：webhook 用 Secrets；自选股/设置项用 Variables；代码统一环境变量+默认值 | 已确认 |
| ADR-0010 | 每轮心跳推送：无信号也推「系统正常」；三态消息互斥；无收市总结 | 已确认 |
| ADR-0011 | 抓取容错：照抄参考项目——串行 + 随机2~5s限速 + 指数退避重试 3 次尝试 | 已确认 |
| ADR-0012 | 手动触发与测试：workflow_dispatch + dry-run（不推送）；Python 3.11 + requirements.txt 照抄参考项目 | 已确认 |
| ADR-0013 | 复盘模式：replay flag 回放最近完整交易日全部信号；复用实时判定逻辑；消息去偏离 | 已确认 |
| ADR-0014 | 多渠道推送：企业微信 + 飞书（interactive 卡片/lark_md + text 降级 + 可选加签）；有哪个推哪个 | 已确认 |
| ADR-0015 | 数据源策略：GitHub runner 东财被封（nid 403 + 断连）→ K线新浪优先东财兜底（KLINE_SOURCE_PRIORITY）+ 东财补丁 + nid 失败缓存 | 已确认 |
| ADR-0016 | 检测粒度改每 5 分钟：实时价 vs MA60（15分钟K线口径不变）；锚点法无状态上穿判定；接受重复推送（最多 3 条） | 已确认（待实现） |

## 事实约束（已查证）

- GitHub Actions `schedule` cron 最小粒度 5 分钟（可精确到分钟），job 启动有 30~60 秒延迟；**高负载时段（整点）可能延迟数分钟，且 schedule 运行失败无自动重试**。
- **仓库 60 天无 activity 时，GitHub 自动禁用 scheduled workflows**（本项目不做 keepalive，用户每日查看运行结果，接受风险）。
- AKShare `stock_zh_a_hist_min_em`（东方财富源）分钟数据仅保留最近约 5 个交易日；高频密集调用会出现 HTTP 429，需指数退避重试。判定上穿需 ≥61 根历史K线（60 根算 MA + 1 根作上一根对比，ADR-0002/0004）。
- **东方财富对云服务器 IP（GitHub Actions runner）风控断连**：nid 令牌接口（anonflow2）403 + 直连 RemoteDisconnected，已被实测确认。本项目 K 线默认**新浪优先、东财兜底**（ADR-0015，`KLINE_SOURCE_PRIORITY=sina,em`）；名称映射走新浪批量接口 `hq.sinajs.cn`。
- 企业微信机器人 Webhook 限制：每分钟最多 20 条消息；单条 text 消息 ≤2048 字节、markdown ≤4096 字节。
- 飞书自定义机器人 Webhook 限制：每分钟最多 100 条消息；支持 interactive 卡片（lark_md 不支持 `#` 标题语法，需转换）与 text；可选加签（HMAC-SHA256）。
- A股每个交易日有 16 根 15 分钟K线。
